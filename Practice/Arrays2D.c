#include<stdio.h>
int main(){
    int x,y;
    printf("\nEnter the number of rows: ");
    scanf("%d", &x);
    printf("\nEnter the number of columns: ");
    scanf("%d", &y);
    int a[x] , b[y];
    
 return 0;   
}